# tiny-api

Tiny in-memory API used by the juks-003 fixture.

## Endpoints

- `listThings()` — returns the current list.
- `addThing(name)` — appends a thing, returns the new length.

When you add or remove an endpoint, document it here.
