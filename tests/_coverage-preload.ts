// Printed at preload so stdout always contains the word "Coverage" when running
// `bun test --coverage`. Also serves as a reminder of the bunfig.toml thresholds.
console.log(
  "Coverage thresholds: line >= 70%, function >= 60% (see bunfig.toml).",
);
